//! Ports `samHeaders.cpp` — builds the SAM/BAM header from `Genome`
//! (chromosome names/lengths), `Parameters.outSAMheaderHD`, PG/CO lines and
//! the optional passthrough of external SAM/BAM headers.

// TODO(M3): pub fn build_sam_header(...) -> Vec<u8>
